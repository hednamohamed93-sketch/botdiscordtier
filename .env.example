# Replit already injects these as real environment variables via its Secrets
# manager -- you do NOT need to fill in a real .env file to run this bot here.
# This file only documents which variables the bot expects.
DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=
DATABASE_URL=
